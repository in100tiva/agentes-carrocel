# Imagens de capa (referência global — insta-mvp)

Arquivos reutilizáveis para o **slide 1 (capa)** do carrossel.

| Arquivo | Uso |
|---------|-----|
| `Corvo-Surpreso.png` | Fundo sugerido da capa (estilo editorial / reação “surpresa”). O **usuário confirma** no checkpoint **step-07c** se usa esta imagem ou outra (`selected-cover-image.md`). |

Os slides **2…N** continuam com o **carrossel infinito** (`bg-NN.png` / Patricia), salvo orientação contrária.
